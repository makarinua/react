// export const Card = ({ text }) => {
//     return <div>{text}</div>;
//   };

//   export default Card;

  export  const Card = ({ children }) => {
    return <div>{children}</div>;
  };

  export default Card;